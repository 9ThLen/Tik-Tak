# The artifacts themselves stay out of git — a large ONNX file in history is
# forever. What is committed is the manifest with their pinned checksums:
# the repository records which bytes are trusted, not the bytes.
*.pt
*.onnx
*.ckpt

# Interrupted atomic pin/save operations may leave recovery files behind.
.*.tmp
.*.backup
.*.lock
